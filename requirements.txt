librosa>=0.10.0
crepe>=0.0.12
numpy>=1.23
pretty_midi>=0.2.9
mido>=1.2.10
scikit-learn>=1.3
soundfile>=0.12.1
torch>=2.0.0  # instale com suporte CUDA se usar GPU

# Recomendado para compatibilidade com CREPE e separação
torchvision>=0.15
torchaudio>=2.0

# Se for usar Demucs via linha de comando
# Instale separadamente: demucs via pip ou conda
